on:
  repository_dispatch:
    types: [test_result]

jobs:
  show_params:
    runs-on: ubuntu-latest
    steps:
      - env:
          MESSAGE: ${{ github.event.client_payload.firebase_service_account }}
        run: echo $MESSAGE
